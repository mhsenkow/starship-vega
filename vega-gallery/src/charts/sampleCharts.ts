import { ChartConfig } from '../types/chart';
import { chartSpecs } from './index';

export const sampleCharts: ChartConfig[] = [
  // Move all chart configurations here from GalleryGrid.tsx
  // This will make GalleryGrid.tsx cleaner and more focused
]; 