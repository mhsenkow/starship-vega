import { DatasetMetadata } from '../../../types/dataset';
import { styled } from 'styled-components';

// Simplified version for gallery use
export const GallerySelector = ({ 
  chartId, 
  currentDataset, 
  onSelect, 
  customDatasets 
}: Omit<DatasetSelectorProps, 'detectDataTypes' | 'setCustomDatasets'>) => {
  // Simplified implementation for gallery view
}; 